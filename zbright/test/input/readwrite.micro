PROGRAM expr
BEGIN

	INT a,b,c,d,e;
	STRING newline := "\n";

	FUNCTION VOID main()
	BEGIN
          READ(c, d);
          
          WRITE(c, newline, d, newline, e, newline);

          a := 1 + 2;

	END	
END